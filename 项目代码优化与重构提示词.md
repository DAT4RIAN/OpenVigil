# 任务：全面优化并重构当前项目代码

请直接扫描当前项目代码，在**不破坏现有功能、不随意改变业务逻辑、不进行无必要的大规模架构重写**的前提下，对项目进行系统性代码优化和重构。

你的任务不是只给建议，而是：

**发现问题 → 制定方案 → 修改代码 → 运行验证 → 修复问题 → 再次检查 → 完成重构。**

---

## 一、总体目标

重点提升：

1. 代码可读性
2. 代码可维护性
3. 模块化程度
4. 复用性
5. 类型安全
6. 异常处理能力
7. 性能
8. 安全性
9. 测试覆盖
10. 项目结构合理性
11. 前后端接口一致性
12. 长期扩展能力

优先解决真实存在的问题，不要为了“看起来高级”而引入复杂设计。

---

# 二、第一阶段：扫描项目

首先阅读：

- README
- package.json / pyproject.toml / requirements.txt
- Dockerfile
- docker-compose.yml
- 环境变量配置
- 数据库模型
- API 路由
- Service 层
- Utils
- 前端页面
- Components
- Hooks
- Store
- Types
- 测试代码
- 配置文件
- CI/CD
- docs

然后快速建立项目结构和依赖关系。

重点查找以下问题。

---

## 1. 重复代码

检查：

- 重复函数
- 重复组件
- 重复 API 调用
- 重复数据库查询
- 重复数据转换逻辑
- 重复异常处理
- 重复表单代码
- 重复状态管理
- copy-paste 代码

对于明显重复逻辑：

优先抽取为：

- 公共函数
- Service
- Hook
- Component
- Utility
- Middleware
- Repository
- 公共类型

但不要过度抽象。

---

## 2. 超大文件和超大函数

重点检查：

- 超过 300～500 行的文件
- 超过 50～100 行的函数
- 一个函数承担多个职责
- 一个组件包含过多业务逻辑
- API Controller 中直接写大量业务代码

按照职责合理拆分。

例如：

Controller  
↓  
Service  
↓  
Repository / DAO  
↓  
Database

前端：

Page  
↓  
Feature Component  
↓  
Common Component  
↓  
Hook / Service / Store

---

## 3. 不合理耦合

查找：

- 页面直接操作底层数据库逻辑
- Controller 包含复杂业务规则
- Service 之间循环依赖
- UI 和业务逻辑高度绑定
- 全局变量滥用
- 模块互相直接访问内部实现
- 前后端数据模型不统一

降低模块之间的隐式依赖。

---

# 三、代码质量优化

检查并处理：

## 命名

修复：

- a / b / temp / data1
- 模糊函数名
- 模糊变量名
- 名称与实际功能不符
- 命名风格不统一

名称应该表达业务含义。

---

## Magic Number / Magic String

例如：

```text
status === 3
type === "1"
timeout = 10000
```

如果属于业务常量，应统一定义。

但不要机械地把所有字符串都抽成常量。

---

## 无效代码

删除：

- 未使用 import
- 未使用变量
- 未使用函数
- 注释掉的旧代码
- 永远不会执行的分支
- 废弃组件
- 重复配置
- 调试 console.log
- 临时测试代码

除非这些代码仍被动态调用或存在兼容性需求。

---

# 四、异常处理

全面检查：

- try/catch
- Promise rejection
- 数据库异常
- 网络异常
- LLM API异常
- 文件IO异常
- JSON解析异常
- 用户输入异常
- 第三方服务异常

重点避免：

```text
catch(Exception) {
}
```

或者：

```text
except Exception:
    pass
```

错误应该：

- 被正确捕获
- 被记录
- 返回合理错误信息
- 保留必要上下文
- 避免泄露敏感信息

需要时增加统一异常处理机制。

---

# 五、数据库优化

检查：

- N+1查询
- 循环查询数据库
- 重复查询
- 无索引字段
- 大量全表扫描
- 不必要的 JOIN
- 不合理事务范围
- 事务遗漏
- Connection 未释放
- ORM 使用错误
- 分页错误
- 批量操作被写成逐条操作

对于明确存在的性能问题直接优化。

不要在没有证据的情况下进行过早优化。

---

# 六、API 重构

检查：

- REST API 命名
- Request 参数
- Response 结构
- HTTP 状态码
- 参数校验
- DTO / Schema
- API错误处理
- API权限
- 分页
- 排序
- 搜索
- 请求超时
- 重试逻辑

尽量保持已有 API 兼容。

除非存在严重设计问题，不要随意修改现有 API 路径。

---

# 七、前端重构

如果项目存在前端，重点检查：

## React / Vue组件

处理：

- 超大组件
- 重复组件
- Props层层传递
- 状态混乱
- useEffect滥用
- 重复请求
- 不必要重新渲染
- 错误的memo/useMemo/useCallback
- Loading状态缺失
- Error状态缺失
- Empty状态缺失

---

## 前端结构

优先形成类似：

```text
pages/
features/
components/
hooks/
services/
stores/
types/
utils/
```

但尊重当前项目已有结构，不要为了目录结构而重构目录。

---

# 八、类型系统

如果使用 TypeScript：

重点减少：

```typescript
any
unknown
as any
```

完善：

- API类型
- DTO
- Props
- Store
- 数据库模型
- 公共类型

优先从真实数据结构推导类型。

不要为了消除 `any` 引入更危险的强制类型断言。

---

# 九、安全检查

检查：

- SQL注入
- XSS
- CSRF
- 命令注入
- 路径穿越
- 文件上传漏洞
- SSRF
- 越权访问
- IDOR
- 明文密码
- API Key写死
- Token泄露
- 日志输出敏感信息
- 不安全反序列化
- 任意文件读写

发现明确安全漏洞时直接修复。

---

# 十、性能优化

关注真正可能形成瓶颈的位置：

- 大循环
- 重复计算
- 重复HTTP请求
- 重复数据库访问
- 大量同步IO
- 大文件一次性读入内存
- 无限制并发
- 无分页列表
- React无意义重新渲染
- 重复LLM调用
- 重复Embedding
- 重复RAG检索

如果不存在明确性能问题，不要为了性能增加复杂度。

---

# 十一、LLM / Agent项目专项检查

如果项目包含 LLM、RAG、Agent，请额外检查：

### LLM

- Prompt是否散落
- Model配置是否写死
- Timeout
- Retry
- Rate Limit
- Token限制
- Streaming异常
- JSON结构化输出
- 模型调用日志
- Cost统计

### RAG

- 文档切片
- Embedding调用
- Vector Store
- Metadata
- Top-K
- Rerank
- 引用来源
- 缓存
- 重复Embedding

### Agent

检查：

```text
任务创建
↓
任务分解
↓
工具调用
↓
状态更新
↓
异常恢复
↓
任务完成
```

重点防止：

- 无限循环
- 重复调用
- 状态丢失
- 并发冲突
- 工具调用失败后无恢复
- Agent之间职责重叠
- 上下文无限增长

---

# 十二、测试

每完成一组修改，就运行相关测试。

至少检查：

- 单元测试
- API测试
- 核心业务流程
- TypeScript检查
- lint
- build

例如：

```bash
npm run lint
npm run typecheck
npm run test
npm run build
```

或者：

```bash
pytest
ruff check .
mypy .
```

根据项目实际技术栈执行，不要机械运行不存在的命令。

---

# 十三、重构原则

严格遵守：

## 原则1：行为保持

默认情况下：

> Refactor ≠ Rewrite

重构后业务行为应该保持一致。

---

## 原则2：小步修改

优先：

```text
发现问题
→ 修改
→ 测试
→ 再修改
```

不要一次性重写整个项目。

---

## 原则3：不要过度设计

不要为了重构而随意引入：

- DDD
- CQRS
- Event Sourcing
- 微服务
- 消息队列
- Kubernetes
- Redis
- 新数据库
- 新框架
- 大量设计模式

只有存在明确收益时才能引入。

---

## 原则4：优先修复高价值问题

优先级：

### P0

- 无法运行
- 数据损坏
- 安全漏洞
- 严重逻辑错误

### P1

- 明显Bug
- 并发问题
- 数据库事务问题
- API错误
- 异常处理缺失

### P2

- 重复代码
- 模块耦合
- 超大函数
- 类型问题

### P3

- 命名
- 注释
- 格式
- 小型性能优化

按照 P0 → P1 → P2 → P3 顺序处理。

---

# 十四、禁止事项

不要：

- 为了重构而重写整个项目
- 擅自改变核心业务逻辑
- 擅自删除已有功能
- 擅自修改公开 API
- 擅自修改数据库Schema
- 无理由升级依赖
- 无理由更换框架
- 无理由增加第三方库
- 制造几十个只有几行代码的小文件
- 为简单逻辑建立复杂抽象层
- 只写重构报告而不修改代码

---

# 十五、执行流程

按照以下顺序执行：

```text
Step 1
扫描项目

Step 2
理解项目架构和核心业务流程

Step 3
识别P0/P1问题

Step 4
修复P0/P1

Step 5
运行测试

Step 6
处理重复代码和模块耦合

Step 7
拆分超大文件/函数

Step 8
完善类型和异常处理

Step 9
优化数据库/API/前端

Step 10
运行完整测试

Step 11
执行Lint/Typecheck/Build

Step 12
检查git diff

Step 13
检查是否引入回归

Step 14
修复遗漏问题

Step 15
输出最终报告
```

---

# 十六、Git Diff自检

完成修改后，必须检查：

```bash
git status
git diff
```

逐项确认：

- 是否误删代码
- 是否改变业务行为
- 是否存在未完成TODO
- 是否引入重复代码
- 是否出现新的any
- 是否存在调试日志
- 是否泄露Secret
- 是否修改了无关文件
- 是否存在不必要格式化导致的大面积diff

如果发现问题，继续修复。

---

# 十七、最终输出

完成实际代码修改后，最后只需要总结：

## 本轮发现

### P0
- xxx

### P1
- xxx

### P2
- xxx

### P3
- xxx

## 已完成修改

- xxx
- xxx
- xxx

## 关键重构

### 文件
`path/to/file`

原问题：

xxx

重构方式：

xxx

收益：

xxx

## 验证结果

```text
Lint:
Typecheck:
Test:
Build:
```

## 未解决问题

只列出本轮无法安全解决或需要业务决策的问题。

---

# 最重要要求

不要停留在分析阶段。

**在理解项目后直接修改代码。**

除非涉及无法判断的核心业务规则，否则自行选择最稳妥方案继续执行，不要因为普通工程细节频繁询问用户。

整个过程中遵循：

> 最小必要修改 + 行为保持 + 可验证 + 可回滚 + 降低复杂度。

最终目标不是让代码“看起来重构过”，而是让项目：

**更稳定、更清晰、更容易测试、更容易维护、更容易继续开发。**