# OpenVigil Product Requirements

本文件是 OpenVigil 当前阶段的产品范围与验收 Source of Truth。它定义“要解决什么问题、为谁解决、做到什么程度”；技术实现、审计问题和执行进度分别以架构、审核与进度文件为准。

## 1. 文档信息

| 字段                   | 内容                                                     |
| ---------------------- | -------------------------------------------------------- |
| Product                | OpenVigil Multi-Agent Platform                           |
| PRD Version            | 1.0                                                      |
| Product Stage          | 生产候选收敛与发布验收准备                               |
| Owner                  | Product                                                  |
| Last Updated           | 2026-09-03                                               |
| Evidence Baseline      | 当前仓库 HEAD `8fd126672776aff6c52328a621391e4f8747627a` |
| Current Product Status | `NOT ACCEPTED`；仍是生产候选，不是已上线生产系统         |

当前状态判断采用以下事实：

- README 与代码均明确区分 `demo` 和 `production` 两套运行边界；
- `EXECUTION_PROGRESS.md` 在 2026-08-29 记录过 CARE 完成，但更晚的 `AUDIT_REPORT.md` 在 2026-08-31 以对抗性和真实 PostgreSQL 复验重新打开 3 个 Critical、6 个 High；
- 当前公开模型激活 HTTP 路径仍未形成审核要求的 anomaly 服务端授权闭环，CI 也尚未纳入 CARE benchmark 完整依赖与三条 PostgreSQL 验收文件；
- 因此，本 PRD 描述目标状态，不把“已有页面”或“历史测试曾通过”视为产品已经验收。

## 2. 产品定义

### 2.1 核心问题

海上风电运维信息分散在资产、SCADA、告警、模型、人员审批、工单、现场证据和知识系统中。运维人员难以快速回答：

1. 当前哪些资产和告警最需要处理？
2. AI 的判断基于什么证据，是否存在反证和不确定性？
3. 谁被允许做出决策，决策是否真正进入了受控执行？
4. 现场任务是否按顺序完成并有可验证证据，设备是否满足恢复条件？
5. 异常检测模型是否经过可复现、无真值泄漏的评估，而不是由演示数据或前端状态伪装为可用？

### 2.2 核心价值

OpenVigil 将“监测 → 告警 → Mission → 诊断 → 人工决策 → 工单 → 现场证据 → 健康复核 → 知识回写”串成一条可追踪、可恢复、受权限约束的闭环，并用 CARE v6 提供独立、受治理的异常检测评估与选择性回放能力。

产品不是以聊天为中心，也不替代人的安全和维护决策。AI 输出的价值在于组织证据、提出可比较方案并推动流程，最终授权和高风险动作仍由具备权限的人完成。

## 3. 目标用户

### 3.1 主要用户

| 用户                                  | 主要任务                                                   | 核心诉求                                               |
| ------------------------------------- | ---------------------------------------------------------- | ------------------------------------------------------ |
| 风场运维值班人员 / Operations Manager | 查看全场态势、处置告警、创建或升级 Mission、安排工单和资源 | 快速定位高风险对象，知道下一步由谁处理，并能追踪到闭环 |
| 运行审批人 / Operations Approver      | 审阅证据和候选方案，批准或拒绝执行                         | 在证据、风险和影响明确的前提下承担人工决策责任         |
| 维护复核人 / Maintenance Reviewer     | 发现诊断或计划不足时请求修订                               | 保证方案可执行且关键问题得到补充分析                   |
| 现场技术人员 / Field Technician       | 查看受控任务，按顺序提交测量与制品证据                     | 明确任务、安全约束和验收阈值，避免无证据关单           |
| 可靠性 / 模型工程师                   | 管理数据集、评估、模型版本、部署、回滚和诊断溯源           | 证明模型结果可复现、无真值泄漏、没有夸大能力           |

“可靠性 / 模型工程师”是业务 persona，不是当前代码中的独立 RBAC 角色。现有权限模型要求承担该职责的人同时具备 `operations_manager`、`model.manage` 和对应全局数据 scope；本阶段不虚构新的角色。

### 3.2 次要用户

- 平台运维与安全人员：检查运行时、配置、审计、可观测性、备份恢复和发布证据。
- 内部审核与管理人员：查看决策、工单、知识案例和确定性报告，不直接改变现场状态。
- SCADA 与 EAM 系统身份：仅执行受限的机器到机器接入或回调，不作为交互式用户。

## 4. 本阶段 Goals

### G-001 — 形成可完成的核心运维闭环

授权用户能够从全场态势和告警进入同一资产上下文，查看有来源的诊断与方案，经人工审批后形成工单，并以顺序化现场证据和健康门槛完成闭环。

### G-002 — 让 AI 结论可解释、可追溯、可否决

每个重要诊断、推荐方案和执行结果都能追溯到资产、时间窗、输入证据、模型或 Agent 版本、工具调用和人工动作；只展示公开、结构化的依据，不展示或伪造隐藏 Chain-of-Thought。

### G-003 — 使 CARE v6 成为可信的模型验证路径

从获授权的只读 CARE v6 数据源出发，建立可信根、质量语义、真值隔离、场内评估、模型门禁、许可证和制品血缘，并把选定事件以受控方式接入平台闭环。

### G-004 — 建立诚实的 Demo / Production 边界

Demo 用于确定性展示；Production 只显示权威后端数据，任何配置、身份、依赖或 API 失败都不得回退到 fixture、固定 WT-023 故事或伪成功状态。

### G-005 — 达到可审计的发布候选标准

关闭当前审核中的全部 Critical / High 产品阻断项，并在干净环境、单库升级、真实依赖、浏览器和发布链上留下可复核证据。完成本阶段并不等同于已经生产上线。

## 5. Non-Goals / Out of Scope

本阶段明确不做：

- 不直接控制风机、启停设备或绕过既有 SCADA / EAM 安全联锁；
- 不允许 AI 自主批准维护方案、绕过人工审批或代替现场安全判断；
- 不承诺生产上线、商业 SLA 或真实风场效果，生产发布仍需外部环境和业务方验收；
- 不建设新的通用数据平台，也不提供浏览器内全量 CARE 原始宽表浏览或训练；
- 不把全量 CARE 信号展开写入 TimescaleDB，只允许选择性的有界回放；
- 不宣称 CARE 可以验证 RUL、30 天故障概率、气象窗口、海况、资源调度或高频振动频谱；
- 不基于匿名传感器编号宣称跨风场泛化；在版本化 ontology 和人工审核完成前，跨场能力保持禁用；
- 不把匿名时间解释为真实场站时间、季节或跨事件先后；
- 不把数字孪生页面宣传为物理仿真、实时控制或工程级三维模型；
- 不在本阶段新增原生移动 App、对外客户门户、计费、市场或多语言能力；响应式 Web 验收仍在范围内；
- 不为了补齐页面数量新增没有数据来源、权限模型或闭环价值的入口。

## 6. 运行模式与权威边界

| 维度     | Demo                                               | Production                                                                   |
| -------- | -------------------------------------------------- | ---------------------------------------------------------------------------- |
| 目的     | 产品演示、截图和回归                               | 生产候选联合验收与最终运行                                                   |
| 数据     | 明确标记的确定性 fixture、Worker/D1 状态和模拟事件 | FastAPI 及其 PostgreSQL/TimescaleDB、Redis、MinIO、Neo4j、模型服务等权威来源 |
| 身份     | 演示 principal，不代表生产 SSO/RBAC                | Sites 每用户委托身份、后端角色与数据范围共同授权                             |
| 写入     | 仅允许文档明确支持的演示闭环写入                   | 只允许公开 API、服务端权限、幂等和 revision 门禁通过的写入                   |
| 失败行为 | 可显示演示模式自身的持久化或离线限制               | 失败关闭；不得显示 Demo 数据作为替代                                         |
| 产品声明 | “可交互演示”                                       | 只有全部发布门禁通过后才可声明“已生产上线”                                   |

PostgreSQL 是生产业务事实的权威来源；Neo4j 是可重建的知识图谱投影，不是交易权威。模型、报告、现场证据和 CARE 制品必须保留不可变身份与摘要。前端缓存或乐观状态不能替代服务器确认。

## 7. Core User Journeys

### Journey P-001 — 从全场态势到可处理 Mission

#### User Goal

值班人员识别最高优先级风险，理解告警影响和证据，并把问题交给可追踪的 Mission。

#### Preconditions

- 用户已认证，并拥有目标租户、风场、资产及所需数据域的查看权限；
- Production 已连接权威后端；若未连接，系统必须明确失败而不是展示演示数据。

#### Main Flow

1. 用户进入运营指挥中心，查看全场 KPI、功率与健康分布、高优告警、活跃 Mission 和 Agent Activity。
2. 用户从风场、风机、SCADA、健康或告警视图筛选目标对象。
3. 用户打开告警或资产详情，查看时间、严重度、测点、健康变化、关联证据及已有 Mission。
4. 有权限的用户确认或指派告警；若尚无 Mission，Operations Manager 从该告警创建 Mission。
5. 系统持久化关联关系、操作者、时间、revision、correlation 和审计事件。
6. 用户进入 Mission 查看分析状态、公开 Agent 事件、证据、失败上下文和下一步动作。

#### Alternative / Failure Flow

- 没有匹配数据时，显示“当前范围为空”或“筛选结果为空”，并提供清除筛选入口；
- 数据过期或实时流中断时，保留最后一次有效读结果但标记时间和 `STALE/OFFLINE`，禁止依赖陈旧 revision 的写入；
- 用户无权执行动作时不提交请求，并说明需要的能力；越权 API 请求必须失败；
- 未知或越界对象不泄露其存在性，按不适用或 `404` 处理；
- 创建请求超时或冲突时不得生成重复 Mission，用户可以使用同一幂等请求安全重试。

#### Success Result

一个真实、唯一、可审计的 Mission 与原告警和资产关联，且用户能继续进入诊断和人工决策。

### Journey P-002 — 从诊断和人工决策到现场闭环

#### User Goal

审批人与现场人员在不跳过证据、安全和职责分离的情况下完成维护任务。

#### Preconditions

- Mission 已产生结构化诊断、证据和候选方案；
- 用户分别具备审批、修订、升级、排程或现场任务权限。

#### Main Flow

1. 审阅者查看异常输入、诊断假设、支持与反证、方案比较、风险、资源和天气约束。
2. 对应角色选择 `approve`、`reject`、`request revision` 或 `escalate`，并提交有效理由。
3. 只有有效批准才允许 Mission 进入执行准备并授权工单；拒绝、修订和升级不得被复用为批准。
4. Operations Manager 在资源和时间窗满足时排程工单。
5. Field Technician 按受控顺序完成任务，为每项任务提交制品 URI、SHA-256、结构化测量和身份归属。
6. 系统逐项验证 schema、对象存在性、摘要、阈值和任务顺序。
7. 所有任务与最终健康门槛通过后，系统完成工单和 Mission，更新告警/健康状态并形成知识案例。

#### Alternative / Failure Flow

- 缺少证据、测量不合格、任务顺序错误或对象摘要不匹配时，当前任务保持未完成并给出可修复原因；
- 资源、天气或排程冲突时，不允许把工单展示为可执行；
- revision 冲突时必须刷新权威状态并重新确认，不可覆盖他人动作；
- 外部 EAM 暂时失败时保留明确的交付状态并允许幂等重试，不得把本地成功冒充为外部已接收。

#### Success Result

用户可以从已关闭的告警或知识案例反查完整的 Mission、决策、审批、工单、现场证据和健康复核链。

### Journey P-003 — 从 CARE 数据集到受治理模型与平台回放

#### User Goal

模型负责人证明异常模型来自可信数据与协议，并把一个合格模型的选定事件接入业务闭环验证。

#### Preconditions

- 运行者有权访问 CARE v6 只读源和专用制品存储；
- 用户拥有 `model.manage` / `view.models` / `view.platform` 等相应能力；
- 批准的数据根、质量规则、特征集、评分协议和许可证元数据可用。

#### Main Flow

1. 用户在数据中心查看 CARE 数据集版本、事件、质量摘要、映射和制品血缘。
2. 离线 Worker 按批准根生成宽表 Parquet、独立质量 mask、冻结预测和评估结果。
3. 模型管理展示场内留一资产评估、失败与不可评分事件、阈值、版本矩阵和服务端 gate 结论。
4. Operations Manager 只能通过公开 HTTP 流程激活 gate 通过且制品未失效的 anomaly 部署；必要时可用同一安全语义回滚。
5. 用户选择一个有界事件窗口和已批准 Avg 信号创建独立 BenchmarkReplayRun。
6. 系统用合成回放时间接入 SCADA 与 anomaly 推理，生成可追溯 prediction；达到告警策略时进入 Alarm → Mission → Diagnosis → Decision。
7. 用户在诊断中心查看 prediction、模型、阈值、质量、原始 source row / anonymous time 和业务对象关系。

#### Alternative / Failure Flow

- 未获 `benchmark_truth` scope 时不查询、不返回、不暗示事件真值和故障描述；
- 根、映射、质量策略、mask、制品摘要、模型绑定或评估状态不一致时，在任何写入或激活前失败；
- 正常事件未满足告警策略时不得为了演示而制造 Alarm 或 Mission；
- 评估失败、不可评分或未达门槛时保留记录并明确阻断原因，不允许隐藏失败候选；
- 回放暂停、重试或重启后从持久 checkpoint 恢复，同一 run 不产生重复样本、prediction、Alarm 或 Mission。

#### Success Result

从批准 CARE 根到模型、部署、prediction、Alarm 和 Mission 的血缘可复核，且产品没有产生跨场、RUL 或真实时间的越界声明。

### Journey P-004 — 发布负责人做出 Go / No-Go 决策

#### User Goal

发布负责人基于同一候选版本的完整证据判断是否允许进入生产，而不是根据页面可打开或测试数量做决定。

#### Main Flow

1. 固定 commit、依赖锁、release ID、镜像摘要和目标环境。
2. 在干净环境执行前后端、CARE、迁移、真实依赖、安全、恢复、性能和浏览器门禁，任何 skip 都按失败处理。
3. 验证 Production 的身份、数据范围、22 个工作区、读写闭环、事件流、报告、指标和追踪。
4. 汇总内容寻址证据，并运行最终 release gate。
5. 所有门禁通过才记录 Go；否则保持 No-Go 和具体阻断项。

#### Success Result

发布结论绑定同一不可变候选版本，可由独立审核人复核，且不会把 Demo 或旧证据当作生产证据。

## 8. Functional Requirements

### PRD-001 — 统一态势感知与跨对象导航

Priority: `P0`

Related Journeys: `P-001`

#### Requirement

产品必须在一个一致的资产上下文中呈现风场、风机、SCADA、健康、告警、活跃 Mission 和 Agent Activity，并允许用户沿真实关联关系进入对象详情。

#### Business Rules

1. 所有 KPI、计数和状态必须来自同一运行模式下的同一权威快照或可说明的聚合窗口。
2. 所有时间都必须显示时区或明确的数据时间语义，并展示数据新鲜度。
3. Production 不得混入 fixture、固定 WT-023 数据或模拟实时事件。
4. 健康度、风险、RUL 和异常分数是不同指标，不得互相替代或使用同一标签误导用户。
5. 数字孪生仅作为资产结构和运行上下文视图，不提供物理仿真承诺。

#### Acceptance Criteria

- [ ] 授权用户可从首页识别高优先级告警和活跃 Mission，并在不丢失资产上下文的情况下进入告警、风机或 Mission 详情。
- [ ] 风场、风机、SCADA、健康和告警视图对同一对象的 ID、当前状态、时间与关联数量一致；不一致时明确标记数据窗口差异。
- [ ] 实时流中断时页面显示最后更新时间和陈旧状态，不把旧值展示为实时值。
- [ ] 首次加载、刷新、无数据、筛选为空、局部失败、整页失败和无权限均有可理解且可操作的状态。
- [ ] Production API 失败时不显示 Demo 资产、告警、天气、Agent 或通知作为回退。
- [ ] 未知对象显示明确未找到；越界对象不暴露名称、状态或是否存在。
- [ ] 桌面与窄屏 Web 均能完成核心查看和导航，键盘焦点、状态文本和图表替代信息可用。

### PRD-002 — 可追溯 Mission、诊断与人工决策

Priority: `P0`

Related Journeys: `P-001`, `P-002`, `P-003`

#### Requirement

系统必须把每个运维问题组织为可追踪的 Mission，呈现公开的 Agent 协作、证据、诊断和候选方案，并通过角色受控的人工结果决定后续状态。

#### Business Rules

1. Mission 必须关联资产，并在由告警触发时关联来源告警或 prediction。
2. 诊断必须区分事实、模型输出、假设、支持证据、反证和建议动作。
3. 展示 Agent 的公开输入、工具调用、输出摘要、失败上下文和版本，不展示或生成隐藏推理过程。
4. `approve`、`reject`、`request revision`、`escalate` 是不同业务结果；后三者不能授权工单执行。
5. 人工理由、操作者、时间、revision 和关联证据必须持久化且不可由请求体伪造身份。

#### Acceptance Criteria

- [ ] Operations Manager 可从授权范围内的告警创建唯一 Mission；同一幂等请求重试不会创建第二个 Mission。
- [ ] Mission 详情能反查来源资产/告警/prediction，并展示按时间排序的公开事件、证据、决策和执行准备度。
- [ ] 诊断结论均带来源或明确标记为假设；缺少证据时不能显示为已证实结论。
- [ ] Approver 可批准或拒绝，Maintenance Reviewer 可请求修订，Operations Manager 可升级；服务端拒绝角色越权。
- [ ] 请求修订后产生新的分析 revision；升级后仍保持待审；拒绝后没有可执行授权。
- [ ] 并发或陈旧 revision 的动作失败且不覆盖已提交结果，用户收到刷新并复核的明确指引。
- [ ] Agent 或模型失败会留下有界、非敏感的公开失败上下文；Mission 不会无提示停在“运行中”。

### PRD-003 — 审批门禁、工单执行与证据闭环

Priority: `P0`

Related Journeys: `P-002`

#### Requirement

系统必须保证只有有效人工批准才能进入工单执行，现场任务必须按受控计划和证据门槛完成，最终关闭必须由实测健康结果支持。

#### Business Rules

1. 工单只有在关联 Decision 获得有效批准后才能进入可排程状态。
2. 只有已排程工单可以开始，只有进行中工单可以提交现场任务证据。
3. 任务按计划顺序完成；每项至少包含制品 URI、64 位十六进制 SHA-256、非空结构化测量和服务端确认的操作者身份。
4. 主轴承默认方案维持现有五步受控模板；具体测量阈值属于版本化计划，UI 不得硬编码另一套规则。
5. 所有任务通过仍不足以自动关单；最终健康/恢复阈值也必须通过。
6. 资源、天气、EAM 和证据存储状态必须显示真实结果，外部系统失败不得被本地状态掩盖。

#### Acceptance Criteria

- [ ] 未批准、被拒绝、请求修订或升级中的 Decision 均无法授权工单执行。
- [ ] Operations Manager 能查看资源/天气准备度并排程合格工单；冲突或数据缺失时系统说明阻断原因。
- [ ] Field Technician 不能跳过前序任务，也不能用缺失、schema 不符、阈值不合格或摘要不匹配的证据完成任务。
- [ ] 重复提交同一任务只产生一次业务结果；不同内容复用幂等键被拒绝。
- [ ] 现场证据在 Production 中经过真实对象存在性与 SHA-256 校验，不能使用 Demo 的 `fixture://` 验证冒充。
- [ ] 全部任务和健康门槛通过后，工单与 Mission 状态一致地完成，并产生可反查的健康复核和知识案例。
- [ ] EAM 交付/回调失败、重试和成功均有独立状态与审计，不产生重复外部工单。

### PRD-004 — CARE v6 数据、评估与模型治理

Priority: `P0`

Related Journeys: `P-003`, `P-004`

#### Requirement

产品必须提供从 CARE v6 可信数据根到评估、模型和部署的可复现治理链，并在数据语义、真值隔离、质量策略、许可证或门槛不成立时失败关闭。

#### Business Rules

1. CARE 原始 ZIP/CSV 只读、不得进入 Git 或就地修改；派生制品写入项目制品目录或专用 CARE 对象存储。
2. 批准根不能仅由待验证文档自哈希证明；真值、映射、质量策略或来源被修改后，即使重新计算全部下游哈希也必须被拒绝。
3. 全信号保留为分区宽表 Parquet；模型只读取冻结的特征集，在线库不接收全量宽表展开。
4. 原值与质量 mask 分离。每个模型族必须声明 `apply` 或 `ignore` mask；忽略时必须有绑定同一输入的影响分析和批准依据。
5. 训练、校准、推理和普通浏览不能读取 prediction 真值；只有最终 evaluator 和获授权的评估后查看流程可以访问。
6. 首版只支持单事件与场内 leave-one-asset-out 评估；跨风场保持禁用。
7. CARE 输出不得包含伪造 RUL、30 天故障概率、真实场站时间或未经证实的根因结论。
8. 数据与受许可证约束的派生分发制品遵循 CC BY-SA 4.0 的归属、许可链接、变更说明和 ShareAlike 复核；OpenVigil 自有源码维持 MIT 边界。
9. anomaly 激活和回滚只接受服务端从权威评估、制品和审批状态生成的一次性授权；客户端不得提交自称通过的指标或 capability。

#### Acceptance Criteria

- [ ] 当前 CARE v6 合同可从唯一批准源重建，并核对 101 个 CSV、95 个事件、36 个源资产、5,242,948 行、45 anomaly / 50 normal；A/B/C 信号列分别为 81/252/952，未知或歧义映射失败。
- [ ] 交换事件真值、翻转质量策略、替换列映射或来源引用后，即使重签全部文档，也会在写入前失败。
- [ ] B/C 状态连续段和信号佐证由生产全量路径计算；短段、长段、跨 batch、缺口和事件边界均有确定结果，不能全部停留在默认值。
- [ ] 模型制品记录实际 mask、规则版本、命中/处理数量和策略；替换、漏读或伪造 mask 计数会被 verifier 拒绝。
- [ ] 同一权威数据库可按 minimal import → offline evaluation → vertical slice → full-scale 顺序升级并重复运行，历史记录保留、写入幂等、并发无重复冲突。
- [ ] 场内评估包含所有请求事件的成功、失败与不可评分结果；UI 不隐藏失败候选，也不把提前量标记为 RUL。
- [ ] 公开 HTTP API 能在不由测试或客户端自行签发授权的情况下完成合格 anomaly 部署的激活和允许的回滚；所有负向与并发场景无部分流量切换。
- [ ] 干净克隆按锁定依赖可收集并运行 CARE 单元与 PostgreSQL 测试；制品的 commit、lock 和 source tree identity 与实际生成来源一致。
- [ ] 数据、模型、预测、评估、激活与导出记录均可追溯到同一批准根和许可证声明。

### PRD-005 — 有界回放与 prediction 到运维闭环

Priority: `P0`

Related Journeys: `P-003`

#### Requirement

系统必须将获批准的 CARE 事件窗口作为独立、可恢复的 BenchmarkReplayRun 接入现有 SCADA 和运维工作流，并保持源时间、合成时间、运行身份和告警来源清晰可辨。

#### Business Rules

1. 每个回放 run、事件和在线资产实例隔离；不同事件或 run 不得拼成一条在线时间序列。
2. 只允许元数据确认的 Avg 信号和有界窗口进入在线回放。
3. source row、anonymous source time、synthetic replay time、sequence、质量规则和模型窗口均须保留。
4. prediction 是正式告警来源，不得制造假的 SCADA ingest receipt。
5. 告警触发、恢复、冷却、缺失、质量不确定和乱序行为由版本化策略决定，并能从持久状态恢复。
6. 同一 run 重试必须复用 checkpoint 和幂等身份；不同内容不能覆盖已有结果。

#### Acceptance Criteria

- [ ] A 场一个异常事件和一个正常事件可完成真实 PostgreSQL 垂直切片；异常链产生可追溯 prediction/Alarm/Mission，正常链不制造告警。
- [ ] 诊断视图能展示模型、部署、score、binary、threshold、窗口、source row、anonymous time、synthetic time、质量摘要及 prediction → Alarm → Mission → Decision 关系。
- [ ] 暂停、进程重启和请求重试后可继续同一 run，且不新增重复样本、prediction、Alarm 或 Mission；新 run 保持独立。
- [ ] 未获真值权限时 SQL 选择与响应均不包含事件标签、故障描述或真值区间；获权后也必须明确这是评估后信息。
- [ ] 至少三次独立新库运行的回放写入均达到现有发布门槛 `≥50 rows/s`，并保留数据规模、环境、镜像摘要和逐次结果。
- [ ] 五个全量目录路径在连续和幂等重跑中的各自 p95 均达到现有门槛 `≤500 ms`，不得通过缩小数据或放宽阈值规避。
- [ ] 上述 PostgreSQL 验收进入 CARE CI/release job，明确校验测试文件和数量，任何 skip、缺文件、根漂移或性能失败都会阻断发布。

### PRD-006 — 身份、运行可信度与支持工作区

Priority: `P0`

Related Journeys: `P-001`, `P-002`, `P-003`, `P-004`

#### Requirement

系统必须以服务端身份、角色、数据范围和能力共同约束每次读写，并让 Agent、资源、维护计划、知识、图谱、报告、模型和系统设置等支持工作区只呈现真实、可审计的状态。

#### Business Rules

1. 前端能力只用于导航和交互提示，后端授权始终是最终裁决。
2. 用户可见范围是角色能力与租户/风场/资产/实体/数据域 scope 的交集；查询参数不能扩大范围。
3. 所有副作用写入必须使用幂等键；涉及可变对象时还必须校验 revision 或等价并发条件。
4. Production 必须显示运行模式、身份范围、后端 release ID / commit / image identity 和依赖就绪状态；任何不匹配均失败关闭。
5. 知识回答必须引用受治理文档或案例；Neo4j 图谱是只读投影，失败时不影响 PostgreSQL 事实但必须明确降级。
6. 报告是绑定数据窗口和摘要的不可变快照；导出失败不能产生空文件或伪成功记录。
7. Agent 管理、模型管理、平台配置和报告生成属于全局高权限动作，必须具备 Operations Manager 角色和相应全局 scope。

#### Acceptance Criteria

- [ ] Production 导航只展示用户可查看的工作区；直接访问无权路由或 API 时仍由服务器拒绝且不泄露越界数据。
- [ ] 两个不同 scope 的身份读取同一对象时，授权主体只看到范围内数据，越界主体得到不泄露存在性的结果；知识图谱搜索和深度参数不能扩大范围。
- [ ] 委托令牌重放、请求绑定不一致、过期、权限不足、release identity 不匹配或依赖未就绪均失败关闭。
- [ ] Agent、知识、图谱、报告、资源、维护计划、模型和设置页面均覆盖 loading、empty、error、permission 和 retry；一个子系统失败不抹去其他已成功区域。
- [ ] Production 页面及响应不包含 Demo principal、fixture、D1、模拟 WebSocket、固定天气、固定通知或 WT-023 演示状态。
- [ ] 审计记录中的 actor 来自已认证主体，客户端提交的姓名或角色不能改写身份。
- [ ] 22 个现有生产工作区均能在受支持的桌面和窄屏浏览器完成其核心只读任务；有写权限的工作区还需完成对应 P0 写链路。

## 9. Product-Level State Requirements

以下规则适用于所有页面、Drawer、表格、图表、表单和写操作。

| State                    | Required Behavior                                                    | Prohibited Behavior                            |
| ------------------------ | -------------------------------------------------------------------- | ---------------------------------------------- |
| Initial Loading          | 保留 App Shell；在实际加载区域显示明确的加载文案或占位；关联动作禁用 | 空白页、显示旧 fixture、让用户误以为“没有数据” |
| Refreshing               | 已有有效数据可保留，但必须显示刷新中；高风险写入基于最新 revision    | 清空整页、无提示使用旧 revision 写入           |
| Empty                    | 说明当前数据域确实无记录，并提供用户有权限执行的下一步               | 使用错误提示代替空态，或虚构示例数据填充       |
| Filtered Empty           | 明确说明是筛选结果为空，提供清除/调整筛选                            | 让用户误以为系统没有任何业务数据               |
| Success                  | 仅在权威服务确认后显示成功；更新 revision、状态和关联计数            | 只依据乐观前端状态宣告成功                     |
| Full Error               | 显示用户可理解的原因、可用的 correlation/error code 和重试入口       | Production 静默回退 Demo，吞掉错误或无限转圈   |
| Partial Failure          | 保留成功区域，在失败区域就地标记并允许独立重试                       | 因一个卡片失败而伪造整页数据或隐藏其他真实结果 |
| Permission Denied        | 已知且可说明的动作显示所需能力；直接越权请求返回稳定错误             | 暴露受限内容、只在前端禁用而后端放行           |
| Out of Scope / Not Found | 对越界对象隐藏存在性；对普通未知对象提供返回列表入口                 | 泄露对象名称、租户、状态或关联数量             |
| Offline / Stale          | 显示最后成功时间和陈旧标识；阻断依赖实时状态的写入                   | 把旧数据标为实时，或在离线时显示已保存         |
| Conflict                 | 明确提示对象已被更新，刷新权威状态并要求用户复核                     | 自动覆盖或用旧审批理由再次提交                 |
| Long Running             | 显示阶段、进度、最近心跳、可恢复状态；允许的取消须明确后果           | 无上限等待、用页面关闭代表任务取消             |
| Dangerous Action         | 提交前展示目标、预期状态和理由；提交中防重复点击                     | 模糊按钮、无对象上下文的一键高风险变更         |

### Retry Rules

- 只读请求可手动重试；自动重试必须有上限，并尊重服务端 `Retry-After`。
- 写请求仅可在同一业务意图下复用相同幂等键、目标、revision 和请求内容重试。
- 相同幂等键但内容不同必须拒绝；验证错误、权限错误和业务门禁失败不得自动重试。
- 网络超时表示“结果未知”而不是“失败且未写入”；客户端应查询权威状态或安全重放同一幂等请求。
- 重试成功后只产生一次业务副作用和一条可解释的重放结果。

## 10. Permission / Role Requirements

查看权限不由角色名称单独决定，而由后端授予的 `view.*` capability 与数据 scope 决定。下表只定义写权限边界。

| Role                   | Allowed Writes                                                                                           | Explicitly Not Allowed                                                                                     |
| ---------------------- | -------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| `operations_manager`   | 告警确认/指派；创建与评论 Mission；升级；工单排程；资源调整；在全局 scope 下管理 Agent、模型、平台和报告 | 批准/拒绝不能冒充 `operations_approver`；请求修订不能冒充 `maintenance_reviewer`；不能提交现场任务完成证据 |
| `operations_approver`  | 告警确认/指派；评论 Mission；批准或拒绝 Decision                                                         | 创建/升级 Mission、请求修订、工单排程、现场任务完成、全局平台管理                                          |
| `maintenance_reviewer` | 请求修订                                                                                                 | 批准、拒绝、升级、排程、现场任务完成                                                                       |
| `field_technician`     | 评论 Mission；按序完成工单任务并提交证据                                                                 | 审批、升级、排程、模型或平台管理                                                                           |
| `scada_ingestor`       | 对授权 source 执行 SCADA ingest                                                                          | 人员交互式业务写入                                                                                         |
| `eam_integrator`       | 对授权工单执行 EAM 回调                                                                                  | Mission、审批、现场证据和平台管理                                                                          |

通用规则：

- 没有身份、角色、scope 或 capability 时默认拒绝；
- actor、verified-by 和审批人由服务器身份确定，忽略客户端自报身份；
- 生产主体没有知识图谱 scope mapping 时失败关闭；
- 前端隐藏或禁用动作不是安全控制，也不能作为后端授权证据。

## 11. Existing Workspace Scope

本阶段不以“增加页面”为目标。以下 22 个已存在工作区共同服务前述六项需求，验收时必须按真实数据链路检查，而不是只做路由 smoke test。

| Product Area     | Routes                                                                    | Primary Requirement       |
| ---------------- | ------------------------------------------------------------------------- | ------------------------- |
| 态势与资产       | `/`, `/wind-farms`, `/turbines/:id`, `/scada`, `/health`, `/digital-twin` | PRD-001                   |
| 告警与智能运维   | `/alarms`, `/diagnosis`, `/predictive-maintenance`                        | PRD-001, PRD-002, PRD-005 |
| AI 运营          | `/agents`, `/missions`, `/missions/:id`, `/decisions`                     | PRD-002, PRD-006          |
| 执行与资源       | `/work-orders`, `/maintenance`, `/resources`                              | PRD-003                   |
| 知识、数据与报告 | `/knowledge`, `/knowledge-graph`, `/data`, `/reports`                     | PRD-004, PRD-006          |
| 系统治理         | `/models`, `/settings`                                                    | PRD-004, PRD-006          |

## 12. Release Acceptance

### 12.1 Product Acceptance Gates

- [ ] 所有 `P0` Acceptance Criteria 均有客观证据，无未声明的部分完成。
- [ ] P-001、P-002、P-003 在 Production 公开 API 和真实 PostgreSQL 环境端到端完成；测试不得直接调用内部 service 绕过产品路径。
- [ ] P-004 使用同一 commit、lock、release ID 和镜像摘要汇总证据，任一 skip 或旧证据均为失败。
- [ ] `AUDIT_REPORT.md` 当前活动问题 `CARE-C001`、`CARE-C003`、`CARE-C005`、`CARE-H006` 至 `CARE-H011` 全部经复验关闭。
- [ ] Demo 与 Production 的页面、响应、身份、事件和持久化边界通过防串线验证。
- [ ] Loading、Empty、Error、Partial Failure、Permission、Offline/Stale、Conflict 和 Retry 在核心流程中有浏览器证据。
- [ ] 受支持桌面与窄屏浏览器完成键盘、屏幕阅读器、200% 缩放、对比度和视觉回归验收，无未关闭的严重/高影响问题。
- [ ] 真实依赖、迁移、备份恢复、DAST/人工渗透、SLO/告警和 Sites 发布后检查全部形成内容寻址证据。
- [ ] 无假入口、无无效按钮、无 fixture 回退、无伪造指标/RUL/证据/身份、无用前端状态代替后端成功。

### 12.2 Product Status Labels

| Label                  | Meaning                                                                |
| ---------------------- | ---------------------------------------------------------------------- |
| `DEMO READY`           | 确定性 Demo 闭环可用且明确标记，不代表生产能力                         |
| `PRODUCTION CANDIDATE` | 仓库内实现具备，但仍有审核或外部验收阻断                               |
| `RELEASE ACCEPTED`     | 本 PRD P0、当前审核和发布证据门禁全部通过，可进入经批准的发布流程      |
| `PRODUCTION LIVE`      | 已在批准环境发布，并完成身份、数据、写链路、回滚和运行监控的发布后验收 |

当前只允许使用 `PRODUCTION CANDIDATE / NOT ACCEPTED`。在 12.1 全部通过前，不得使用 `RELEASE ACCEPTED` 或 `PRODUCTION LIVE`。

## 13. Open Questions

以下问题在现有仓库中没有充分产品依据，不在本 PRD 中擅自作答；它们会影响最终发布配置或业务验收，但不改变当前功能边界。

1. 首个生产试点的租户、风场、资产范围、用户数量和明确业务 Owner 是什么？
2. 首发必须联调的真实 SCADA/CMS、气象和 EAM 厂商/端点分别是什么，哪些允许延期？
3. 生产身份目录如何映射五类人员角色、数据 scope 与审批职责，谁批准和定期复核权限？
4. 谁拥有 CARE 批准根与质量/mask 策略的签署权，根轮换和紧急吊销由谁审批？
5. anomaly 模型的业务级激活审批人与回滚触发条件是什么；除现有技术门槛外是否还有现场安全门槛？
6. 正式支持的浏览器、屏幕尺寸、辅助技术组合和最低版本是什么？
7. 业务方批准的 RTO、RPO、告警响应目标和生产容量模型是什么？现有工程门槛不自动等同于业务 SLA。

这些问题得到明确答复后，应更新本文件或对应的发布验收配置；在此之前不得通过默认假设扩展产品声明。
